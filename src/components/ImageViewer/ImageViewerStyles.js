const styles = {
  row: {
    alignItems: "center",
  },
  // wrapper: {
  //   height: 600,
  // },
  iconButton: {
    backgroundColor: theme => theme.palette.background.paper,
  },
  openIconWrapper: {
    position: "absolute",
    top: 24,
    right: 8 * 4,
  },
};

export default styles;
